<!DOCTYPE html>
<html>
  <head>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title><%= content_for?(:title) ? yield(:title) : 'i Conference Connect 2' %></title>
    <meta name='description' content='<%= content_for?(:description) ? yield(:description) : 'Learn Rails' %>'>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id='mini-banner' class="navbar navbar-default navbar-fixed-top" >
      <div class="col-md-2 col-md-offset-1">
        i-conf-connect
      </div>
      <div class="col-md-2 col-md-offset-5 email">
        <%= 'Logged in as ' + get_user_name(current_user) if user_signed_in? %>
        <% if !user_signed_in? %>
          <%= link_to "/auth/twitter" do %>
            <% image_tag('twitter.jpeg', height: '30px', class: 'twitter') %>
          <% end %>
        <% end %>
      </div>
      <div class="col-md-2">
        <% if user_signed_in? %>
          <%= link_to 'Logout', destroy_user_session_path, class: 'log-in-out btn-xs', :method => :delete %>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: 'log-in-out btn-xs' %>
        <% end %>
      </div>
    </div>
    <div id='banner' class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="row">
        <% if current_user && current_user.admin? %>
          <%= render 'admin/admin_nav_bar' %>
        <% else %>
          <%= render 'users/user_nav_bar' %>
        <% end %>
      </div>
    </div>
    <main role='main', class='mid body'>
      <div id="message_container">
        <div id="flash_messages">
          <% flash.each do |type, message| %>
            <div class='alert <%= bootstrap_class_for(type) %> fade in'>
              <button class='close' data-dismiss='alert'>×</button>
              <%= message %>
            </div>
          <% end %>
        </div>
      </div>
      <div class='row'>
        <div class='col-md-12'>
          <%= yield %>
        </div>
      </div>
    </main>
  </body>
</html>
